[gd_resource type="Resource" script_class="LandmarkType" load_steps=3 format=3]

[ext_resource type="Script" path="res://data/landmarks/landmark_type.gd" id="1"]
[ext_resource type="PackedScene" path="res://scenes/world/visuals/monolith_visual.tscn" id="2"]

[resource]
script = ExtResource("1")
id = &"standing_stones"
display_name = "The Standing Stones"
description = "Nine slabs set on end by nobody anybody remembers, in the one country where the light pools of its own accord. Wisps drift between them all night and the stones are never warm."
site_count = 1
radius_min = 26.0
radius_max = 84.0
footprint_radius = 4
biome_ids = Array[StringName]([&"leyfield"])
piece_scenes = Array[PackedScene]([ExtResource("2")])
piece_count = 9
ring_angle_jitter = 0.22
ring_radius_jitter = 0.7
arrangement = 0
sight_radius = 15.0
map_color = Color(0.72, 0.76, 1, 1)
