[gd_resource type="Resource" script_class="LandmarkType" load_steps=4 format=3]

[ext_resource type="Script" path="res://data/landmarks/landmark_type.gd" id="1"]
[ext_resource type="PackedScene" path="res://scenes/world/visuals/ruined_spire_visual.tscn" id="2"]
[ext_resource type="PackedScene" path="res://scenes/world/visuals/pillar_visual.tscn" id="3"]

[resource]
script = ExtResource("1")
id = &"ruined_spire"
display_name = "The Broken Spire"
description = "A watchtower like ours, from a village like ours, that did not last the night it was built for. It is the only landmark that stands in any country, because whoever built these built them everywhere — and it keeps to the near ground for the same reason we did."
site_count = 2
radius_min = 20.0
radius_max = 56.0
footprint_radius = 3
piece_scenes = Array[PackedScene]([ExtResource("3")])
centerpiece_scene = ExtResource("2")
piece_count = 4
arrangement = 1
sight_radius = 14.0
map_color = Color(0.85, 0.78, 0.6, 1)
